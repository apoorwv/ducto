---
name: ci-fixes-2025-06-30
description: Fixed 3 CI failures (ruff lint, lease renewal race, H10 store parity) + gitignore .serena
metadata:
  type: project
---

CI run 129 failed on 3 issues:
1. Ruff lint: `tests/test_cli.py` import sorting (I001), `tests/test_engine.py` zip missing strict=True (B905)
2. JS lease renewal test on Node 22: `expireLease` margin too tight (1ms). Changed to 60s.
3. JS H10 store integration test (all Node): full DB wipe at test start instead of per-user cleanup — belt against CI parallel worker races.

Also added `.serena/` to `.gitignore`.

**Why:** CI was red on main, blocking releases.

**How to apply:** Changes already pushed to main.
